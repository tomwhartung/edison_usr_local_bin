#!/usr/bin/python
#
# up: find out what's up
# ----------------------
#
import sys
sys.path.append( '/usr/local/bin/pythonLibs' )
## print( 'sys.path: [%s]' % ', '.join(map(str, sys.path)) )
import tomsUlbFunctions as ulbLib

###################
#
# Initialization
#
if ( len(sys.argv) < 2 ) :
	toMatch = ''
else :
	toMatch = sys.argv[1]

print( 'toMatch: "' + toMatch + '"' )

###################
#
#  Mainline code
#

## ulbLib.getMatchingProcesses( toMatch )

isYoctoLinux = ulbLib.isYoctoLinux()

print( 'isYoctoLinux  = ' + str(isYoctoLinux) )

exitVal = 0

exit( exitVal )
